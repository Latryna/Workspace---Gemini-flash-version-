### TITANS handshake test pack

### 1. Test LM chat via local agent
POST http://localhost:5051/chat
Content-Type: application/json

{
  "model": "mixtral-8x7b-instruct-v0.1",
  "messages": [
    { "role": "system", "content": "You are a helpful assistant." },
    { "role": "user", "content": "Hello from the handshake test pack." }
  ]
}

### 2. Test local directory listing
POST http://localhost:5051/local/dir
Content-Type: application/json

{
  "path": "."
}

### 3. Test reading a file locally
POST http://localhost:5051/local/read
Content-Type: application/json

{
  "path": "README.md"
}

### 4. Test running a simple shell command
POST http://localhost:5051/local/run
Content-Type: application/json

{
  "cmd": "echo handshake_test && ls -la"
}

### 5. Test GitHub repository contents via GitHub API
# This call fetches the contents of the root of the titans‑legal‑docs repository.
GET https://api.github.com/repos/Latryna/titans-legal-docs/contents/

### 6. Example orchestrator message targeting a cloud model
# This request uses the local chat endpoint to send a message tagged for a cloud model.
# In a real orchestration scenario, the Orchestrator would route this to OpenRouter.
POST http://localhost:5051/chat
Content-Type: application/json

{
  "model": "mixtral-8x7b-instruct-v0.1",
  "messages": [
    { "role": "system", "content": "You are a local orchestrator test agent." },
    { "role": "user", "content": "[CLOUD:Claude] Provide a one‑sentence summary of the TITANS project." }
  ]
}