# Example environment variables for the TITANS AI Workspace

# API key for OpenRouter chat completions.  Do not check real keys
# into version control; instead set the value in your local environment
# or CI/CD secrets store.
VITE_OPENROUTER_API_KEY=